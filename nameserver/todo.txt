Test create function when file with same name exists

add a thread for checking servers state(ping each 10 seconds or something)
check all query outputs 
change db.my_collection.find(query) commands into db.my_collection.find_one(query)

check what is the value of request fields when value for that field is not passed

check read dir command

How deletion of directory happens on storage server
Should I send all the subsequent directories in the folder to delete or top directory is enough?